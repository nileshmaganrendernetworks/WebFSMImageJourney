{
  "name": "lantern-festival",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/logic.test.js",
    "test:ui": "playwright test",
    "verify": "npm run test && npm run test:ui"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "playwright": "^1.56.0",
    "vite": "^8.3.0"
  }
}
